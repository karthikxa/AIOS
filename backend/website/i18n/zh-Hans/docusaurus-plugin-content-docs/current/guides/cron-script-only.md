---
sidebar_position: 13
title: "çº¯è„šæœ¬ Cron ä»»åŠ¡ï¼ˆæ—  LLMï¼‰"
description: "å®Œå…¨è·³è¿‡ LLM çš„ç»å…¸çœ‹é—¨ç‹— cron ä»»åŠ¡â€”â€”è„šæœ¬æŒ‰è®¡åˆ’è¿è¡Œï¼Œå…¶ stdout è¾“å‡ºç›´æŽ¥æŠ•é€’åˆ°ä½ çš„æ¶ˆæ¯å¹³å°ã€‚å†…å­˜å‘Šè­¦ã€ç£ç›˜å‘Šè­¦ã€CI é€šçŸ¥ã€å®šæœŸå¥åº·æ£€æŸ¥ã€‚"
---

# çº¯è„šæœ¬ Cron ä»»åŠ¡

æœ‰æ—¶ä½ å·²ç»æ¸…æ¥šåœ°çŸ¥é“è¦å‘é€ä»€ä¹ˆæ¶ˆæ¯ã€‚ä½ ä¸éœ€è¦ agent æ¥æŽ¨ç†â€”â€”ä½ åªéœ€è¦ä¸€ä¸ªè„šæœ¬æŒ‰è®¡æ—¶å™¨è¿è¡Œï¼Œå¹¶å°†å…¶è¾“å‡ºï¼ˆå¦‚æœ‰ï¼‰å‘é€åˆ° Telegram / Discord / Slack / Signalã€‚

Zed å°†æ­¤ç§°ä¸º**æ—  agent æ¨¡å¼**ã€‚è¿™æ˜¯åŽ»æŽ‰ LLM çš„ cron ç³»ç»Ÿã€‚

<!-- ascii-guard-ignore -->
```
   â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”          â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
   â”‚ scheduler tick   â”‚  every   â”‚ run script       â”‚
   â”‚ (every N minutes)â”‚ â”€â”€â”€â”€â”€â”€â–¶ â”‚ (bash or python) â”‚
   â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜          â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
                                          â”‚
                                          â”‚ stdout
                                          â–¼
                                 â”Œâ”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”
                                 â”‚ delivery router  â”‚
                                 â”‚ (telegram/discâ€¦) â”‚
                                 â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”˜
```
<!-- ascii-guard-ignore-end -->

- **æ—  LLM è°ƒç”¨ã€‚** é›¶ tokenï¼Œé›¶ agent å¾ªçŽ¯ï¼Œé›¶æ¨¡åž‹è´¹ç”¨ã€‚
- **è„šæœ¬å³ä»»åŠ¡ã€‚** ç”±è„šæœ¬å†³å®šæ˜¯å¦å‘Šè­¦ã€‚æœ‰è¾“å‡º â†’ å‘é€æ¶ˆæ¯ï¼›æ— è¾“å‡º â†’ é™é»˜æ‰§è¡Œã€‚
- **Bash æˆ– Pythonã€‚** `.sh` / `.bash` æ–‡ä»¶åœ¨ `/bin/bash` ä¸‹è¿è¡Œï¼›å…¶ä»–æ‰©å±•ååœ¨å½“å‰ Python è§£é‡Šå™¨ä¸‹è¿è¡Œã€‚`~/.zed/scripts/` ä¸­çš„ä»»ä½•æ–‡ä»¶å‡å¯æŽ¥å—ã€‚
- **åŒä¸€è°ƒåº¦å™¨ã€‚** ä¸Ž LLM ä»»åŠ¡å…±å­˜äºŽ `cronjob` ä¸­â€”â€”æš‚åœã€æ¢å¤ã€åˆ—å‡ºã€æ—¥å¿—å’ŒæŠ•é€’ç›®æ ‡çš„æ“ä½œæ–¹å¼å®Œå…¨ç›¸åŒã€‚

## é€‚ç”¨åœºæ™¯

ä»¥ä¸‹æƒ…å†µä½¿ç”¨æ—  agent æ¨¡å¼ï¼š

- **å†…å­˜ / ç£ç›˜ / GPU çœ‹é—¨ç‹—ã€‚** æ¯ 5 åˆ†é’Ÿè¿è¡Œä¸€æ¬¡ï¼Œä»…åœ¨è¶…è¿‡é˜ˆå€¼æ—¶å‘Šè­¦ã€‚
- **CI hookï¼ˆé’©å­ï¼‰ã€‚** éƒ¨ç½²å®Œæˆ â†’ å‘é€ commit SHAï¼›æž„å»ºå¤±è´¥ â†’ å‘é€æœ€åŽ 100 è¡Œæ—¥å¿—ã€‚
- **å®šæœŸæŒ‡æ ‡ã€‚** "æ¯å¤©ä¸Šåˆ 9 ç‚¹çš„ Stripe æ”¶å…¥"â€”â€”ä¸€æ¬¡ç®€å•çš„ API è°ƒç”¨åŠ æ ¼å¼åŒ–è¾“å‡ºã€‚
- **å¤–éƒ¨äº‹ä»¶è½®è¯¢ã€‚** æ£€æŸ¥ APIï¼Œåœ¨çŠ¶æ€å˜åŒ–æ—¶å‘Šè­¦ã€‚
- **å¿ƒè·³ã€‚** æ¯ N åˆ†é’Ÿ ping ä¸€æ¬¡ä»ªè¡¨æ¿ï¼Œè¯æ˜Žä¸»æœºå­˜æ´»ã€‚

å½“ä½ éœ€è¦ agent **å†³å®š**è¯´ä»€ä¹ˆæ—¶â€”â€”æ€»ç»“é•¿æ–‡æ¡£ã€ä»Ž feed ä¸­æŒ‘é€‰æœ‰è¶£æ¡ç›®ã€èµ·è‰å‹å¥½æé†’â€”â€”è¯·ä½¿ç”¨æ™®é€šçš„ï¼ˆLLM é©±åŠ¨çš„ï¼‰cron ä»»åŠ¡ã€‚æ—  agent è·¯å¾„é€‚ç”¨äºŽè„šæœ¬çš„ stdout æœ¬èº«å°±æ˜¯æ¶ˆæ¯å†…å®¹çš„åœºæ™¯ã€‚

## é€šè¿‡èŠå¤©åˆ›å»º

æ—  agent æ¨¡å¼çš„çœŸæ­£ä¼˜åŠ¿åœ¨äºŽï¼šagent æœ¬èº«å¯ä»¥ä¸ºä½ è®¾ç½®çœ‹é—¨ç‹—â€”â€”æ— éœ€ç¼–è¾‘å™¨ã€æ— éœ€ shellã€æ— éœ€è®°å¿† CLI å‚æ•°ã€‚ä½ æè¿°éœ€æ±‚ï¼ŒZed ç¼–å†™è„šæœ¬ã€å®‰æŽ’è®¡åˆ’ï¼Œå¹¶å‘ŠçŸ¥ä½ ä½•æ—¶è§¦å‘ã€‚

### ç¤ºä¾‹å¯¹è¯

> **ä½ ï¼š** æ¯ 5 åˆ†é’Ÿæ£€æŸ¥ä¸€æ¬¡ï¼Œå¦‚æžœå†…å­˜è¶…è¿‡ 85% å°±åœ¨ telegram é€šçŸ¥æˆ‘
>
> **Zedï¼š** *ï¼ˆå†™å…¥ `~/.zed/scripts/memory-watchdog.sh`ï¼Œç„¶åŽä»¥ `no_agent=true` è°ƒç”¨ `cronjob(...)`ï¼‰*
>
> å·²è®¾ç½®ã€‚æ¯ 5 åˆ†é’Ÿè¿è¡Œä¸€æ¬¡ï¼Œä»…åœ¨å†…å­˜è¶…è¿‡ 85% æ—¶å‘Šè­¦ Telegramã€‚è„šæœ¬ï¼š`memory-watchdog.sh`ã€‚ä»»åŠ¡ IDï¼š`abc123`ã€‚

åœ¨åº•å±‚ï¼Œagent è¿›è¡Œäº†ä¸¤æ¬¡å·¥å…·è°ƒç”¨ï¼š

```python
# 1. å†™å…¥æ£€æŸ¥è„šæœ¬
write_file(
    path="~/.zed/scripts/memory-watchdog.sh",
    content='''#!/usr/bin/env bash
ram_pct=$(free | awk '/^Mem:/ {printf "%d", $3 * 100 / $2}')
if [ "$ram_pct" -ge 85 ]; then
  echo "RAM ${ram_pct}% on $(hostname)"
fi
# Empty stdout = silent tick; no message sent.
''',
)

# 2. è°ƒåº¦ä»»åŠ¡ â€” no_agent=True åœ¨æ¯æ¬¡è§¦å‘æ—¶è·³è¿‡ LLM
cronjob(
    action="create",
    schedule="every 5m",
    script="memory-watchdog.sh",
    no_agent=True,
    deliver="telegram",
    name="memory-watchdog",
)
```

æ­¤åŽæ¯æ¬¡è§¦å‘éƒ½æ˜¯å…è´¹çš„ï¼šè°ƒåº¦å™¨è¿è¡Œè„šæœ¬ï¼Œè‹¥ stdout éžç©ºåˆ™å°†å…¶å‘é€åˆ° Telegramï¼Œä¸”ä»Žä¸è°ƒç”¨æ¨¡åž‹ã€‚

### agent ä¸ºä½ å†³å®šçš„å†…å®¹

å½“ä½ çš„è¯·æ±‚ç±»ä¼¼"å½“ X æ—¶å‘Šè­¦æˆ‘"æˆ–"æ¯ N åˆ†é’Ÿæ£€æŸ¥ Yï¼Œå¦‚æžœ Z åˆ™é€šçŸ¥æˆ‘"ï¼ŒZed çš„ `cronjob` å·¥å…·æè¿°ä¼šæŒ‡ç¤ºå®ƒåœ¨æ¶ˆæ¯å†…å®¹å®Œå…¨ç”±è„šæœ¬å†³å®šæ—¶ä½¿ç”¨ `no_agent=True`ã€‚å½“è¯·æ±‚éœ€è¦æŽ¨ç†æ—¶ï¼ˆ*"æ€»ç»“æ–° issue"*ã€*"æŒ‘é€‰æœ€æœ‰è¶£çš„å¤´æ¡"*ã€*"èµ·è‰ä¸€æ¡å‹å¥½æé†’"*ï¼‰ï¼Œåˆ™å›žé€€åˆ°æ™®é€šçš„ LLM é©±åŠ¨è·¯å¾„ã€‚

ä½ ä¸å¿…è‡ªå·±æŒ‡å®š `--no-agent`ã€‚åªéœ€æè¿°ä½ æƒ³è¦çš„è¡Œä¸ºå³å¯ã€‚

### é€šè¿‡èŠå¤©ç®¡ç†çœ‹é—¨ç‹—

agent å¯ä»¥ç”¨ä¸Žåˆ›å»ºä»»åŠ¡ç›¸åŒçš„æ–¹å¼æš‚åœã€æ¢å¤ã€ç¼–è¾‘å’Œåˆ é™¤ä»»åŠ¡ï¼š

> **ä½ ï¼š** ä»Šæ™šå…ˆåœæŽ‰å†…å­˜çœ‹é—¨ç‹—
>
> **Zedï¼š** *ï¼ˆè°ƒç”¨ `cronjob(action='pause', job_id='abc123')`ï¼‰*
>
> å·²æš‚åœã€‚è¯´"é‡æ–°å¼€å¯"æˆ–æ‰§è¡Œ `zed cron resume abc123` å¯æ¢å¤ã€‚

> **ä½ ï¼š** æ”¹æˆæ¯ 15 åˆ†é’Ÿä¸€æ¬¡
>
> **Zedï¼š** *ï¼ˆè°ƒç”¨ `cronjob(action='update', job_id='abc123', schedule='every 15m')`ï¼‰*

å®Œæ•´ç”Ÿå‘½å‘¨æœŸï¼ˆåˆ›å»º / åˆ—å‡º / æ›´æ–° / æš‚åœ / æ¢å¤ / ç«‹å³è¿è¡Œ / åˆ é™¤ï¼‰å‡å¯ç”± agent æ“ä½œï¼Œæ— éœ€ä½ å­¦ä¹ ä»»ä½• CLI å‘½ä»¤ã€‚

## é€šè¿‡ CLI åˆ›å»º

åå¥½ shellï¼ŸCLI è·¯å¾„ç”¨ä¸‰æ¡å‘½ä»¤å³å¯è¾¾åˆ°ç›¸åŒæ•ˆæžœï¼š

```bash
# 1. ç¼–å†™è„šæœ¬
cat > ~/.zed/scripts/memory-watchdog.sh <<'EOF'
#!/usr/bin/env bash
# Alert when RAM usage is over 85%. Silent otherwise.
RAM_PCT=$(free | awk '/^Mem:/ {printf "%d", $3 * 100 / $2}')
if [ "$RAM_PCT" -ge 85 ]; then
  echo "âš  RAM ${RAM_PCT}% on $(hostname)"
fi
# Empty stdout = silent run; no message sent.
EOF
chmod +x ~/.zed/scripts/memory-watchdog.sh

# 2. è°ƒåº¦ä»»åŠ¡
zed cron create "every 5m" \
  --no-agent \
  --script memory-watchdog.sh \
  --deliver telegram \
  --name "memory-watchdog"

# 3. éªŒè¯
zed cron list
zed cron run <job_id>    # è§¦å‘ä¸€æ¬¡ä»¥æµ‹è¯•
```

å°±è¿™äº›ã€‚æ—  promptï¼ˆæç¤ºè¯ï¼‰ï¼Œæ— æŠ€èƒ½ï¼Œæ— æ¨¡åž‹ã€‚


## è„šæœ¬è¾“å‡ºä¸ŽæŠ•é€’çš„æ˜ å°„å…³ç³»

| è„šæœ¬è¡Œä¸º | ç»“æžœ |
|-----------------|--------|
| é€€å‡ºç  0ï¼Œstdout éžç©º | stdout åŽŸæ ·æŠ•é€’ |
| é€€å‡ºç  0ï¼Œstdout ä¸ºç©º | é™é»˜æ‰§è¡Œâ€”â€”ä¸æŠ•é€’ |
| é€€å‡ºç  0ï¼Œstdout æœ€åŽä¸€è¡ŒåŒ…å« `{"wakeAgent": false}` | é™é»˜æ‰§è¡Œï¼ˆä¸Ž LLM ä»»åŠ¡å…±ç”¨çš„é—¨æŽ§ï¼‰ |
| éžé›¶é€€å‡ºç  | æŠ•é€’é”™è¯¯å‘Šè­¦ï¼ˆç¡®ä¿æŸåçš„çœ‹é—¨ç‹—ä¸ä¼šé™é»˜å¤±è´¥ï¼‰ |
| è„šæœ¬è¶…æ—¶ | æŠ•é€’é”™è¯¯å‘Šè­¦ |

"ç©ºåˆ™é™é»˜"çš„è¡Œä¸ºæ˜¯ç»å…¸çœ‹é—¨ç‹—æ¨¡å¼çš„å…³é”®ï¼šè„šæœ¬å¯ä»¥æ¯åˆ†é’Ÿè¿è¡Œä¸€æ¬¡ï¼Œä½†åªæœ‰åœ¨çœŸæ­£éœ€è¦å…³æ³¨æ—¶ï¼Œé¢‘é“æ‰ä¼šæ”¶åˆ°æ¶ˆæ¯ã€‚

## è„šæœ¬è§„åˆ™

è„šæœ¬å¿…é¡»ä½äºŽ `~/.zed/scripts/`ã€‚è¿™åœ¨ä»»åŠ¡åˆ›å»ºæ—¶å’Œè¿è¡Œæ—¶å‡ä¼šå¼ºåˆ¶æ£€æŸ¥â€”â€”ç»å¯¹è·¯å¾„ã€`~/` å±•å¼€ä»¥åŠè·¯å¾„ç©¿è¶Šæ¨¡å¼ï¼ˆ`../`ï¼‰å‡ä¼šè¢«æ‹’ç»ã€‚è¯¥ç›®å½•ä¸Ž LLM ä»»åŠ¡ä½¿ç”¨çš„é¢„æ£€è„šæœ¬é—¨æŽ§å…±äº«ã€‚

è§£é‡Šå™¨ç”±æ–‡ä»¶æ‰©å±•åå†³å®šï¼š

| æ‰©å±•å | è§£é‡Šå™¨ |
|-----------|-------------|
| `.sh`ã€`.bash` | `/bin/bash` |
| å…¶ä»–ä»»æ„æ‰©å±•å | `sys.executable`ï¼ˆå½“å‰ Pythonï¼‰ |

æˆ‘ä»¬æœ‰æ„**ä¸**éµå¾ª `#!/...` shebangâ€”â€”ä¿æŒè§£é‡Šå™¨é›†åˆæ˜Žç¡®ä¸”ç²¾ç®€ï¼Œå¯å‡å°‘è°ƒåº¦å™¨ä¿¡ä»»çš„æ”»å‡»é¢ã€‚

## è®¡åˆ’è¯­æ³•

ä¸Žæ‰€æœ‰å…¶ä»– cron ä»»åŠ¡ç›¸åŒï¼š

```bash
zed cron create "every 5m"        # é—´éš”
zed cron create "every 2h"
zed cron create "0 9 * * *"       # æ ‡å‡† cronï¼šæ¯å¤©ä¸Šåˆ 9 ç‚¹
zed cron create "30m"             # å•æ¬¡ï¼š30 åˆ†é’ŸåŽè¿è¡Œä¸€æ¬¡
```

å®Œæ•´è¯­æ³•è¯·å‚é˜… [cron åŠŸèƒ½å‚è€ƒ](/user-guide/features/cron)ã€‚

## æŠ•é€’ç›®æ ‡

`--deliver` æŽ¥å— gateway å·²çŸ¥çš„æ‰€æœ‰ç›®æ ‡ã€‚å¸¸è§å½¢å¼ï¼š

```bash
--deliver telegram                       # å¹³å°é»˜è®¤é¢‘é“
--deliver telegram:-1001234567890        # æŒ‡å®šèŠå¤©
--deliver telegram:-1001234567890:17585  # æŒ‡å®š Telegram è®ºå›è¯é¢˜
--deliver discord:#ops
--deliver slack:#engineering
--deliver signal:+15551234567
--deliver local                          # ä»…ä¿å­˜åˆ° ~/.zed/cron/output/
```

å¯¹äºŽä½¿ç”¨ bot token çš„å¹³å°ï¼ˆTelegramã€Discordã€Slackã€Signalã€SMSã€WhatsAppï¼‰ï¼Œè„šæœ¬è¿è¡Œæ—¶æ— éœ€è¿è¡Œä¸­çš„ gatewayâ€”â€”å·¥å…·ç›´æŽ¥ä½¿ç”¨ `~/.zed/.env` / `~/.zed/config.yaml` ä¸­å·²æœ‰çš„å‡­æ®è°ƒç”¨å„å¹³å°çš„ REST ç«¯ç‚¹ã€‚

## ç¼–è¾‘ä¸Žç”Ÿå‘½å‘¨æœŸ

```bash
zed cron list                                    # æŸ¥çœ‹æ‰€æœ‰ä»»åŠ¡
zed cron pause <job_id>                          # åœæ­¢è§¦å‘ï¼Œä¿ç•™å®šä¹‰
zed cron resume <job_id>
zed cron edit <job_id> --schedule "every 10m"    # è°ƒæ•´é¢‘çŽ‡
zed cron edit <job_id> --agent                   # åˆ‡æ¢ä¸º LLM æ¨¡å¼
zed cron edit <job_id> --no-agent --script â€¦     # åˆ‡æ¢å›žæ—  agent æ¨¡å¼
zed cron remove <job_id>                         # åˆ é™¤ä»»åŠ¡
```

æ‰€æœ‰é€‚ç”¨äºŽ LLM ä»»åŠ¡çš„æ“ä½œï¼ˆæš‚åœã€æ¢å¤ã€æ‰‹åŠ¨è§¦å‘ã€æŠ•é€’ç›®æ ‡å˜æ›´ï¼‰åŒæ ·é€‚ç”¨äºŽæ—  agent ä»»åŠ¡ã€‚

## å®žæˆ˜ç¤ºä¾‹ï¼šç£ç›˜ç©ºé—´å‘Šè­¦

```bash
cat > ~/.zed/scripts/disk-alert.sh <<'EOF'
#!/usr/bin/env bash
# Alert when / or /home is over 90% full.
THRESHOLD=90
df -h / /home 2>/dev/null | awk -v t="$THRESHOLD" '
  NR > 1 && $5+0 >= t {
    printf "âš  Disk %s full on %s\n", $5, $6
  }
'
EOF
chmod +x ~/.zed/scripts/disk-alert.sh

zed cron create "*/15 * * * *" \
  --no-agent \
  --script disk-alert.sh \
  --deliver telegram \
  --name "disk-alert"
```

å½“ä¸¤ä¸ªæ–‡ä»¶ç³»ç»Ÿå‡ä½ŽäºŽ 90% æ—¶é™é»˜ï¼›å½“æŸä¸ªæ–‡ä»¶ç³»ç»Ÿè¶…å‡ºé˜ˆå€¼æ—¶ï¼Œæ¯ä¸ªè¶…é™æ–‡ä»¶ç³»ç»Ÿè§¦å‘ä¸€è¡Œå‘Šè­¦ã€‚

## ä¸Žå…¶ä»–æ¨¡å¼çš„å¯¹æ¯”

| æ–¹å¼ | è¿è¡Œå†…å®¹ | é€‚ç”¨åœºæ™¯ |
|----------|-----------|-------------|
| `cronjob --no-agent`ï¼ˆæœ¬é¡µï¼‰ | ä½ çš„è„šæœ¬ï¼Œç”± Zed è°ƒåº¦ | ä¸éœ€è¦æŽ¨ç†çš„å‘¨æœŸæ€§çœ‹é—¨ç‹— / å‘Šè­¦ / æŒ‡æ ‡ |
| `cronjob`ï¼ˆé»˜è®¤ï¼ŒLLMï¼‰ | å¸¦å¯é€‰é¢„æ£€è„šæœ¬çš„ agent | æ¶ˆæ¯å†…å®¹éœ€è¦å¯¹æ•°æ®è¿›è¡ŒæŽ¨ç†æ—¶ |
| OS cron + `curl` åˆ° [webhook è®¢é˜…](/user-guide/messaging/webhooks) | ä½ çš„è„šæœ¬ï¼Œç”± OS è°ƒåº¦ | å½“ Zed æœ¬èº«å¯èƒ½ä¸å¥åº·æ—¶ï¼ˆå³è¢«ç›‘æŽ§å¯¹è±¡ï¼‰ |

å¯¹äºŽå¿…é¡»åœ¨ **gateway å®•æœºæ—¶ä¹Ÿèƒ½è§¦å‘**çš„å…³é”®ç³»ç»Ÿå¥åº·çœ‹é—¨ç‹—ï¼Œè¯·ä½¿ç”¨ OS çº§ cron é…åˆ `curl` è°ƒç”¨ Zed webhook è®¢é˜…ï¼ˆæˆ–ä»»ä½•å¤–éƒ¨å‘Šè­¦ç«¯ç‚¹ï¼‰â€”â€”è¿™äº›ä½œä¸ºç‹¬ç«‹ OS è¿›ç¨‹è¿è¡Œï¼Œä¸ä¾èµ– Zed æ˜¯å¦åœ¨çº¿ã€‚å½“è¢«ç›‘æŽ§å¯¹è±¡æ˜¯å¤–éƒ¨ç³»ç»Ÿæ—¶ï¼Œin-gateway è°ƒåº¦å™¨æ‰æ˜¯æ­£ç¡®é€‰æ‹©ã€‚

## ç›¸å…³æ–‡æ¡£

- [ç”¨ Cron è‡ªåŠ¨åŒ–ä¸€åˆ‡](/guides/automate-with-cron) â€” LLM é©±åŠ¨çš„ cron æ¨¡å¼ã€‚
- [å®šæ—¶ä»»åŠ¡ï¼ˆCronï¼‰å‚è€ƒ](/user-guide/features/cron) â€” å®Œæ•´è®¡åˆ’è¯­æ³•ã€ç”Ÿå‘½å‘¨æœŸã€æŠ•é€’è·¯ç”±ã€‚
- [Webhook è®¢é˜…](/user-guide/messaging/webhooks) â€” ä¾›å¤–éƒ¨è°ƒåº¦å™¨ä½¿ç”¨çš„å³å‘å³å¿˜ HTTP å…¥å£ã€‚
- [Gateway å†…éƒ¨æœºåˆ¶](/developer-guide/gateway-internals) â€” æŠ•é€’è·¯ç”±å™¨å†…éƒ¨å®žçŽ°ã€‚
