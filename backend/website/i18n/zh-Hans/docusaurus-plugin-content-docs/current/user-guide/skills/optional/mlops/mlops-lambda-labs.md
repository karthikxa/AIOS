---
title: "Lambda Labs Gpu Cloud â€” ç”¨äºŽ ML è®­ç»ƒå’ŒæŽ¨ç†çš„é¢„ç•™åŠæŒ‰éœ€ GPU äº‘å®žä¾‹"
sidebar_label: "Lambda Labs Gpu Cloud"
description: "ç”¨äºŽ ML è®­ç»ƒå’ŒæŽ¨ç†çš„é¢„ç•™åŠæŒ‰éœ€ GPU äº‘å®žä¾‹"
---

{/* This page is auto-generated from the skill's SKILL.md by website/scripts/generate-skill-docs.py. Edit the source SKILL.md, not this page. */}

# Lambda Labs Gpu Cloud

ç”¨äºŽ ML è®­ç»ƒå’ŒæŽ¨ç†çš„é¢„ç•™åŠæŒ‰éœ€ GPU äº‘å®žä¾‹ã€‚å½“ä½ éœ€è¦å…·å¤‡ç®€å• SSH è®¿é—®çš„ä¸“ç”¨ GPU å®žä¾‹ã€æŒä¹…åŒ–æ–‡ä»¶ç³»ç»Ÿï¼Œæˆ–ç”¨äºŽå¤§è§„æ¨¡è®­ç»ƒçš„é«˜æ€§èƒ½å¤šèŠ‚ç‚¹é›†ç¾¤æ—¶ï¼Œè¯·ä½¿ç”¨æ­¤ skillã€‚

## Skill å…ƒæ•°æ®

| | |
|---|---|
| æ¥æº | å¯é€‰ â€” é€šè¿‡ `zed skills install official/mlops/lambda-labs` å®‰è£… |
| è·¯å¾„ | `optional-skills/mlops/lambda-labs` |
| ç‰ˆæœ¬ | `1.0.0` |
| ä½œè€… | Orchestra Research |
| è®¸å¯è¯ | MIT |
| ä¾èµ– | `lambda-cloud-client>=1.0.0` |
| å¹³å° | linux, macos, windows |
| æ ‡ç­¾ | `Infrastructure`, `GPU Cloud`, `Training`, `Inference`, `Lambda Labs` |

## å‚è€ƒï¼šå®Œæ•´ SKILL.md

:::info
ä»¥ä¸‹æ˜¯ Zed åœ¨è§¦å‘æ­¤ skill æ—¶åŠ è½½çš„å®Œæ•´ skill å®šä¹‰ã€‚è¿™æ˜¯ agent åœ¨ skill æ¿€æ´»æ—¶æ‰€çœ‹åˆ°çš„æŒ‡ä»¤å†…å®¹ã€‚
:::

# Lambda Labs GPU Cloud

åœ¨ Lambda Labs GPU äº‘ä¸Šè¿è¡Œ ML å·¥ä½œè´Ÿè½½çš„ç»¼åˆæŒ‡å—ï¼Œæ¶µç›–æŒ‰éœ€å®žä¾‹å’Œ 1-Click Clustersã€‚

## ä½•æ—¶ä½¿ç”¨ Lambda Labs

**åœ¨ä»¥ä¸‹æƒ…å†µä¸‹ä½¿ç”¨ Lambda Labsï¼š**
- éœ€è¦å…·å¤‡å®Œæ•´ SSH è®¿é—®æƒé™çš„ä¸“ç”¨ GPU å®žä¾‹
- è¿è¡Œé•¿æ—¶é—´è®­ç»ƒä»»åŠ¡ï¼ˆæ•°å°æ—¶è‡³æ•°å¤©ï¼‰
- å¸Œæœ›ç®€å•å®šä»·ä¸”æ— å‡ºå£è´¹ç”¨
- éœ€è¦è·¨ä¼šè¯çš„æŒä¹…åŒ–å­˜å‚¨
- éœ€è¦é«˜æ€§èƒ½å¤šèŠ‚ç‚¹é›†ç¾¤ï¼ˆ16-512 ä¸ª GPUï¼‰
- å¸Œæœ›ä½¿ç”¨é¢„è£… ML æ ˆï¼ˆLambda Stackï¼Œå« PyTorchã€CUDAã€NCCLï¼‰

**ä¸»è¦ç‰¹æ€§ï¼š**
- **GPU ç§ç±»**ï¼šB200ã€H100ã€GH200ã€A100ã€A10ã€A6000ã€V100
- **Lambda Stack**ï¼šé¢„è£… PyTorchã€TensorFlowã€CUDAã€cuDNNã€NCCL
- **æŒä¹…åŒ–æ–‡ä»¶ç³»ç»Ÿ**ï¼šå®žä¾‹é‡å¯åŽæ•°æ®ä¿ç•™
- **1-Click Clusters**ï¼š16-512 ä¸ª GPU çš„ Slurm é›†ç¾¤ï¼Œé…å¤‡ InfiniBand
- **ç®€å•å®šä»·**ï¼šæŒ‰åˆ†é’Ÿè®¡è´¹ï¼Œæ— å‡ºå£è´¹ç”¨
- **å…¨çƒåŒºåŸŸ**ï¼šå…¨çƒ 12+ ä¸ªåŒºåŸŸ

**ä»¥ä¸‹æƒ…å†µè¯·ä½¿ç”¨æ›¿ä»£æ–¹æ¡ˆï¼š**
- **Modal**ï¼šç”¨äºŽæ— æœåŠ¡å™¨ã€è‡ªåŠ¨æ‰©ç¼©å®¹å·¥ä½œè´Ÿè½½
- **SkyPilot**ï¼šç”¨äºŽå¤šäº‘ç¼–æŽ’å’Œæˆæœ¬ä¼˜åŒ–
- **RunPod**ï¼šç”¨äºŽæ›´ä¾¿å®œçš„ç«žä»·å®žä¾‹å’Œæ— æœåŠ¡å™¨ç«¯ç‚¹
- **Vast.ai**ï¼šç”¨äºŽä»·æ ¼æœ€ä½Žçš„ GPU å¸‚åœº

## å¿«é€Ÿå¼€å§‹

### è´¦æˆ·è®¾ç½®

1. åœ¨ https://lambda.ai åˆ›å»ºè´¦æˆ·
2. æ·»åŠ ä»˜æ¬¾æ–¹å¼
3. ä»ŽæŽ§åˆ¶å°ç”Ÿæˆ API å¯†é’¥
4. æ·»åŠ  SSH å¯†é’¥ï¼ˆå¯åŠ¨å®žä¾‹å‰å¿…é¡»å®Œæˆï¼‰

### é€šè¿‡æŽ§åˆ¶å°å¯åŠ¨

1. å‰å¾€ https://cloud.lambda.ai/instances
2. ç‚¹å‡»"Launch instance"
3. é€‰æ‹© GPU ç±»åž‹å’ŒåŒºåŸŸ
4. é€‰æ‹© SSH å¯†é’¥
5. å¯é€‰æ‹©æŒ‚è½½æ–‡ä»¶ç³»ç»Ÿ
6. å¯åŠ¨å¹¶ç­‰å¾… 3-15 åˆ†é’Ÿ

### é€šè¿‡ SSH è¿žæŽ¥

```bash
# ä»ŽæŽ§åˆ¶å°èŽ·å–å®žä¾‹ IP
ssh ubuntu@<INSTANCE-IP>

# æˆ–ä½¿ç”¨æŒ‡å®šå¯†é’¥
ssh -i ~/.ssh/lambda_key ubuntu@<INSTANCE-IP>
```

## GPU å®žä¾‹

### å¯ç”¨ GPU

| GPU | æ˜¾å­˜ | ä»·æ ¼/GPU/å°æ—¶ | æœ€é€‚ç”¨åœºæ™¯ |
|-----|------|--------------|----------|
| B200 SXM6 | 180 GB | $4.99 | æœ€å¤§æ¨¡åž‹ï¼Œæœ€å¿«è®­ç»ƒ |
| H100 SXM | 80 GB | $2.99-3.29 | å¤§æ¨¡åž‹è®­ç»ƒ |
| H100 PCIe | 80 GB | $2.49 | æ€§ä»·æ¯” H100 |
| GH200 | 96 GB | $1.49 | å• GPU å¤§æ¨¡åž‹ |
| A100 80GB | 80 GB | $1.79 | ç”Ÿäº§è®­ç»ƒ |
| A100 40GB | 40 GB | $1.29 | æ ‡å‡†è®­ç»ƒ |
| A10 | 24 GB | $0.75 | æŽ¨ç†ã€å¾®è°ƒ |
| A6000 | 48 GB | $0.80 | æ˜¾å­˜/ä»·æ ¼æ¯”ä¼˜ |
| V100 | 16 GB | $0.55 | ä½Žæˆæœ¬è®­ç»ƒ |

### å®žä¾‹é…ç½®

```
8x GPU: æœ€é€‚åˆåˆ†å¸ƒå¼è®­ç»ƒï¼ˆDDPã€FSDPï¼‰
4x GPU: å¤§æ¨¡åž‹ã€å¤š GPU è®­ç»ƒ
2x GPU: ä¸­ç­‰å·¥ä½œè´Ÿè½½
1x GPU: å¾®è°ƒã€æŽ¨ç†ã€å¼€å‘
```

### å¯åŠ¨æ—¶é—´

- å• GPUï¼š3-5 åˆ†é’Ÿ
- å¤š GPUï¼š10-15 åˆ†é’Ÿ

## Lambda Stack

æ‰€æœ‰å®žä¾‹å‡é¢„è£… Lambda Stackï¼š

```bash
# åŒ…å«è½¯ä»¶
- Ubuntu 22.04 LTS
- NVIDIA drivers (latest)
- CUDA 12.x
- cuDNN 8.x
- NCCL (for multi-GPU)
- PyTorch (latest)
- TensorFlow (latest)
- JAX
- JupyterLab
```

### éªŒè¯å®‰è£…

```bash
# æ£€æŸ¥ GPU
nvidia-smi

# æ£€æŸ¥ PyTorch
python -c "import torch; print(torch.cuda.is_available())"

# æ£€æŸ¥ CUDA ç‰ˆæœ¬
nvcc --version
```

## Python API

### å®‰è£…

```bash
pip install lambda-cloud-client
```

### è®¤è¯

```python
import os
import lambda_cloud_client

# ä½¿ç”¨ API å¯†é’¥é…ç½®
configuration = lambda_cloud_client.Configuration(
    host="https://cloud.lambdalabs.com/api/v1",
    access_token=os.environ["LAMBDA_API_KEY"]
)
```

### åˆ—å‡ºå¯ç”¨å®žä¾‹

```python
with lambda_cloud_client.ApiClient(configuration) as api_client:
    api = lambda_cloud_client.DefaultApi(api_client)

    # èŽ·å–å¯ç”¨å®žä¾‹ç±»åž‹
    types = api.instance_types()
    for name, info in types.data.items():
        print(f"{name}: {info.instance_type.description}")
```

### å¯åŠ¨å®žä¾‹

```python
from lambda_cloud_client.models import LaunchInstanceRequest

request = LaunchInstanceRequest(
    region_name="us-west-1",
    instance_type_name="gpu_1x_h100_sxm5",
    ssh_key_names=["my-ssh-key"],
    file_system_names=["my-filesystem"],  # å¯é€‰
    name="training-job"
)

response = api.launch_instance(request)
instance_id = response.data.instance_ids[0]
print(f"Launched: {instance_id}")
```

### åˆ—å‡ºè¿è¡Œä¸­çš„å®žä¾‹

```python
instances = api.list_instances()
for instance in instances.data:
    print(f"{instance.name}: {instance.ip} ({instance.status})")
```

### ç»ˆæ­¢å®žä¾‹

```python
from lambda_cloud_client.models import TerminateInstanceRequest

request = TerminateInstanceRequest(
    instance_ids=[instance_id]
)
api.terminate_instance(request)
```

### SSH å¯†é’¥ç®¡ç†

```python
from lambda_cloud_client.models import AddSshKeyRequest

# æ·»åŠ  SSH å¯†é’¥
request = AddSshKeyRequest(
    name="my-key",
    public_key="ssh-rsa AAAA..."
)
api.add_ssh_key(request)

# åˆ—å‡ºå¯†é’¥
keys = api.list_ssh_keys()

# åˆ é™¤å¯†é’¥
api.delete_ssh_key(key_id)
```

## ä½¿ç”¨ curl çš„ CLI

### åˆ—å‡ºå®žä¾‹ç±»åž‹

```bash
curl -u $LAMBDA_API_KEY: \
  https://cloud.lambdalabs.com/api/v1/instance-types | jq
```

### å¯åŠ¨å®žä¾‹

```bash
curl -u $LAMBDA_API_KEY: \
  -X POST https://cloud.lambdalabs.com/api/v1/instance-operations/launch \
  -H "Content-Type: application/json" \
  -d '{
    "region_name": "us-west-1",
    "instance_type_name": "gpu_1x_h100_sxm5",
    "ssh_key_names": ["my-key"]
  }' | jq
```

### ç»ˆæ­¢å®žä¾‹

```bash
curl -u $LAMBDA_API_KEY: \
  -X POST https://cloud.lambdalabs.com/api/v1/instance-operations/terminate \
  -H "Content-Type: application/json" \
  -d '{"instance_ids": ["<INSTANCE-ID>"]}' | jq
```

## æŒä¹…åŒ–å­˜å‚¨

### æ–‡ä»¶ç³»ç»Ÿ

æ–‡ä»¶ç³»ç»Ÿåœ¨å®žä¾‹é‡å¯åŽä¿ç•™æ•°æ®ï¼š

```bash
# æŒ‚è½½ä½ç½®
/lambda/nfs/<FILESYSTEM_NAME>

# ç¤ºä¾‹ï¼šä¿å­˜æ£€æŸ¥ç‚¹
python train.py --checkpoint-dir /lambda/nfs/my-storage/checkpoints
```

### åˆ›å»ºæ–‡ä»¶ç³»ç»Ÿ

1. å‰å¾€ Lambda æŽ§åˆ¶å°ä¸­çš„ Storage
2. ç‚¹å‡»"Create filesystem"
3. é€‰æ‹©åŒºåŸŸï¼ˆå¿…é¡»ä¸Žå®žä¾‹åŒºåŸŸä¸€è‡´ï¼‰
4. å‘½åå¹¶åˆ›å»º

### æŒ‚è½½åˆ°å®žä¾‹

æ–‡ä»¶ç³»ç»Ÿå¿…é¡»åœ¨å®žä¾‹å¯åŠ¨æ—¶æŒ‚è½½ï¼š
- é€šè¿‡æŽ§åˆ¶å°ï¼šå¯åŠ¨æ—¶é€‰æ‹©æ–‡ä»¶ç³»ç»Ÿ
- é€šè¿‡ APIï¼šåœ¨å¯åŠ¨è¯·æ±‚ä¸­åŒ…å« `file_system_names`

### æœ€ä½³å®žè·µ

<!-- ascii-guard-ignore -->
```bash
# å­˜å‚¨åœ¨æ–‡ä»¶ç³»ç»Ÿä¸Šï¼ˆæŒä¹…åŒ–ï¼‰
/lambda/nfs/storage/
  â”œâ”€â”€ datasets/
  â”œâ”€â”€ checkpoints/
  â”œâ”€â”€ models/
  â””â”€â”€ outputs/

# æœ¬åœ° SSDï¼ˆæ›´å¿«ï¼Œä¸´æ—¶ï¼‰
/home/ubuntu/
  â””â”€â”€ working/  # ä¸´æ—¶æ–‡ä»¶
```
<!-- ascii-guard-ignore-end -->

## SSH é…ç½®

### æ·»åŠ  SSH å¯†é’¥

```bash
# åœ¨æœ¬åœ°ç”Ÿæˆå¯†é’¥
ssh-keygen -t ed25519 -f ~/.ssh/lambda_key

# å°†å…¬é’¥æ·»åŠ åˆ° Lambda æŽ§åˆ¶å°
# æˆ–é€šè¿‡ API æ·»åŠ 
```

### å¤šä¸ªå¯†é’¥

```bash
# åœ¨å®žä¾‹ä¸Šæ·»åŠ æ›´å¤šå¯†é’¥
echo 'ssh-rsa AAAA...' >> ~/.ssh/authorized_keys
```

### ä»Ž GitHub å¯¼å…¥

```bash
# åœ¨å®žä¾‹ä¸Šæ‰§è¡Œ
ssh-import-id gh:username
```

### SSH éš§é“

```bash
# è½¬å‘ Jupyter
ssh -L 8888:localhost:8888 ubuntu@<IP>

# è½¬å‘ TensorBoard
ssh -L 6006:localhost:6006 ubuntu@<IP>

# å¤šç«¯å£
ssh -L 8888:localhost:8888 -L 6006:localhost:6006 ubuntu@<IP>
```

## JupyterLab

### ä»ŽæŽ§åˆ¶å°å¯åŠ¨

1. å‰å¾€ Instances é¡µé¢
2. ç‚¹å‡» Cloud IDE åˆ—ä¸­çš„"Launch"
3. JupyterLab åœ¨æµè§ˆå™¨ä¸­æ‰“å¼€

### æ‰‹åŠ¨è®¿é—®

```bash
# åœ¨å®žä¾‹ä¸Š
jupyter lab --ip=0.0.0.0 --port=8888

# åœ¨æœ¬åœ°æœºå™¨ä¸Šå»ºç«‹éš§é“
ssh -L 8888:localhost:8888 ubuntu@<IP>
# æ‰“å¼€ http://localhost:8888
```

## è®­ç»ƒå·¥ä½œæµ

### å• GPU è®­ç»ƒ

```bash
# SSH åˆ°å®žä¾‹
ssh ubuntu@<IP>

# å…‹éš†ä»“åº“
git clone https://github.com/user/project
cd project

# å®‰è£…ä¾èµ–
pip install -r requirements.txt

# è®­ç»ƒ
python train.py --epochs 100 --checkpoint-dir /lambda/nfs/storage/checkpoints
```

### å¤š GPU è®­ç»ƒï¼ˆå•èŠ‚ç‚¹ï¼‰

```python
# train_ddp.py
import torch
import torch.distributed as dist
from torch.nn.parallel import DistributedDataParallel as DDP

def main():
    dist.init_process_group("nccl")
    rank = dist.get_rank()
    device = rank % torch.cuda.device_count()

    model = MyModel().to(device)
    model = DDP(model, device_ids=[device])

    # è®­ç»ƒå¾ªçŽ¯...

if __name__ == "__main__":
    main()
```

```bash
# ä½¿ç”¨ torchrun å¯åŠ¨ï¼ˆ8 ä¸ª GPUï¼‰
torchrun --nproc_per_node=8 train_ddp.py
```

### æ£€æŸ¥ç‚¹ä¿å­˜åˆ°æ–‡ä»¶ç³»ç»Ÿ

```python
import os

checkpoint_dir = "/lambda/nfs/my-storage/checkpoints"
os.makedirs(checkpoint_dir, exist_ok=True)

# ä¿å­˜æ£€æŸ¥ç‚¹
torch.save({
    'epoch': epoch,
    'model_state_dict': model.state_dict(),
    'optimizer_state_dict': optimizer.state_dict(),
    'loss': loss,
}, f"{checkpoint_dir}/checkpoint_{epoch}.pt")
```

## 1-Click Clusters

### æ¦‚è¿°

é«˜æ€§èƒ½ Slurm é›†ç¾¤ï¼Œå…·å¤‡ï¼š
- 16-512 ä¸ª NVIDIA H100 æˆ– B200 GPU
- NVIDIA Quantum-2 400 Gb/s InfiniBand
- GPUDirect RDMAï¼Œé€ŸçŽ‡ 3200 Gb/s
- é¢„è£…åˆ†å¸ƒå¼ ML æ ˆ

### åŒ…å«è½¯ä»¶

- Ubuntu 22.04 LTS + Lambda Stack
- NCCLã€Open MPI
- PyTorchï¼ˆå« DDP å’Œ FSDPï¼‰
- TensorFlow
- OFED é©±åŠ¨

### å­˜å‚¨

- æ¯ä¸ªè®¡ç®—èŠ‚ç‚¹ 24 TB NVMeï¼ˆä¸´æ—¶ï¼‰
- Lambda æ–‡ä»¶ç³»ç»Ÿç”¨äºŽæŒä¹…åŒ–æ•°æ®

### å¤šèŠ‚ç‚¹è®­ç»ƒ

```bash
# åœ¨ Slurm é›†ç¾¤ä¸Š
srun --nodes=4 --ntasks-per-node=8 --gpus-per-node=8 \
  torchrun --nnodes=4 --nproc_per_node=8 \
  --rdzv_backend=c10d --rdzv_endpoint=$MASTER_ADDR:29500 \
  train.py
```

## ç½‘ç»œ

### å¸¦å®½

- å®žä¾‹é—´ï¼ˆåŒä¸€åŒºåŸŸï¼‰ï¼šæœ€é«˜ 200 Gbps
- äº’è”ç½‘å‡ºç«™ï¼šæœ€é«˜ 20 Gbps

### é˜²ç«å¢™

- é»˜è®¤ï¼šä»…å¼€æ”¾ 22 ç«¯å£ï¼ˆSSHï¼‰
- åœ¨ Lambda æŽ§åˆ¶å°ä¸­é…ç½®å…¶ä»–ç«¯å£
- é»˜è®¤å…è®¸ ICMP æµé‡

### ç§æœ‰ IP

```bash
# æŸ¥æ‰¾ç§æœ‰ IP
ip addr show | grep 'inet '
```

## å¸¸è§å·¥ä½œæµ

### å·¥ä½œæµ 1ï¼šå¾®è°ƒ LLM

```bash
# 1. å¯åŠ¨å¸¦æ–‡ä»¶ç³»ç»Ÿçš„ 8x H100 å®žä¾‹

# 2. SSH å¹¶è®¾ç½®çŽ¯å¢ƒ
ssh ubuntu@<IP>
pip install transformers accelerate peft

# 3. å°†æ¨¡åž‹ä¸‹è½½åˆ°æ–‡ä»¶ç³»ç»Ÿ
python -c "
from transformers import AutoModelForCausalLM
model = AutoModelForCausalLM.from_pretrained('meta-llama/Llama-2-7b-hf')
model.save_pretrained('/lambda/nfs/storage/models/llama-2-7b')
"

# 4. ä½¿ç”¨æ–‡ä»¶ç³»ç»Ÿä¸Šçš„æ£€æŸ¥ç‚¹è¿›è¡Œå¾®è°ƒ
accelerate launch --num_processes 8 train.py \
  --model_path /lambda/nfs/storage/models/llama-2-7b \
  --output_dir /lambda/nfs/storage/outputs \
  --checkpoint_dir /lambda/nfs/storage/checkpoints
```

### å·¥ä½œæµ 2ï¼šæ‰¹é‡æŽ¨ç†

```bash
# 1. å¯åŠ¨ A10 å®žä¾‹ï¼ˆæŽ¨ç†æ€§ä»·æ¯”é«˜ï¼‰

# 2. è¿è¡ŒæŽ¨ç†
python inference.py \
  --model /lambda/nfs/storage/models/fine-tuned \
  --input /lambda/nfs/storage/data/inputs.jsonl \
  --output /lambda/nfs/storage/data/outputs.jsonl
```

## æˆæœ¬ä¼˜åŒ–

### é€‰æ‹©åˆé€‚çš„ GPU

| ä»»åŠ¡ | æŽ¨è GPU |
|------|-----------------|
| LLM å¾®è°ƒï¼ˆ7Bï¼‰ | A100 40GB |
| LLM å¾®è°ƒï¼ˆ70Bï¼‰ | 8x H100 |
| æŽ¨ç† | A10ã€A6000 |
| å¼€å‘ | V100ã€A10 |
| æœ€é«˜æ€§èƒ½ | B200 |

### é™ä½Žæˆæœ¬

1. **ä½¿ç”¨æ–‡ä»¶ç³»ç»Ÿ**ï¼šé¿å…é‡å¤ä¸‹è½½æ•°æ®
2. **é¢‘ç¹ä¿å­˜æ£€æŸ¥ç‚¹**ï¼šæ¢å¤ä¸­æ–­çš„è®­ç»ƒ
3. **åˆç†é…ç½®**ï¼šä¸è¦è¿‡åº¦åˆ†é… GPU
4. **ç»ˆæ­¢ç©ºé—²å®žä¾‹**ï¼šæ— è‡ªåŠ¨åœæ­¢ï¼Œéœ€æ‰‹åŠ¨ç»ˆæ­¢

### ç›‘æŽ§ä½¿ç”¨æƒ…å†µ

- æŽ§åˆ¶å°æ˜¾ç¤ºå®žæ—¶ GPU åˆ©ç”¨çŽ‡
- é€šè¿‡ API è¿›è¡Œç¨‹åºåŒ–ç›‘æŽ§

## å¸¸è§é—®é¢˜

| é—®é¢˜ | è§£å†³æ–¹æ¡ˆ |
|-------|----------|
| å®žä¾‹æ— æ³•å¯åŠ¨ | æ£€æŸ¥åŒºåŸŸå¯ç”¨æ€§ï¼Œå°è¯•ä¸åŒ GPU |
| SSH è¿žæŽ¥è¢«æ‹’ç» | ç­‰å¾…å®žä¾‹åˆå§‹åŒ–ï¼ˆ3-15 åˆ†é’Ÿï¼‰ |
| ç»ˆæ­¢åŽæ•°æ®ä¸¢å¤± | ä½¿ç”¨æŒä¹…åŒ–æ–‡ä»¶ç³»ç»Ÿ |
| æ•°æ®ä¼ è¾“ç¼“æ…¢ | ä½¿ç”¨åŒä¸€åŒºåŸŸçš„æ–‡ä»¶ç³»ç»Ÿ |
| GPU æœªè¢«æ£€æµ‹åˆ° | é‡å¯å®žä¾‹ï¼Œæ£€æŸ¥é©±åŠ¨ |

## å‚è€ƒèµ„æ–™

- **[é«˜çº§ç”¨æ³•](https://github.com/zedteam/zed-agent/blob/main/optional-skills/mlops/lambda-labs/references/advanced-usage.md)** â€” å¤šèŠ‚ç‚¹è®­ç»ƒã€API è‡ªåŠ¨åŒ–
- **[æ•…éšœæŽ’æŸ¥](https://github.com/zedteam/zed-agent/blob/main/optional-skills/mlops/lambda-labs/references/troubleshooting.md)** â€” å¸¸è§é—®é¢˜åŠè§£å†³æ–¹æ¡ˆ

## èµ„æº

- **æ–‡æ¡£**ï¼šhttps://docs.lambda.ai
- **æŽ§åˆ¶å°**ï¼šhttps://cloud.lambda.ai
- **å®šä»·**ï¼šhttps://lambda.ai/instances
- **æ”¯æŒ**ï¼šhttps://support.lambdalabs.com
- **åšå®¢**ï¼šhttps://lambda.ai/blog
