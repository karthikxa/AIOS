name: web-xai
version: 1.0.0
description: "xAI Web Search â€” search the web via Grok's agentic web_search tool (Responses API). Requires xAI Grok OAuth (via `zed auth`) or XAI_API_KEY (https://x.ai)."
author: NousResearch
kind: backend
provides_web_providers:
  - xai
