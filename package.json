{
  "name": "avde-unified",
  "version": "1.0.0",
  "description": "Single-command bootloader for AVDE",
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --prefix \"[{name}]\" --names \"BACKEND,DESKTOP,FRONTEND\" --prefix-colors \"bgBlue.bold,bgMagenta.bold,bgCyan.bold\" \"npm run dev:backend\" \"npm run dev:desktop\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && .venv\\Scripts\\python.exe server.py",
    "dev:desktop": "cd desktop-agent && python start_agent.py",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
