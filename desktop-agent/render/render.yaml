services:
  - type: web
    name: desktop-agent
    runtime: docker
    dockerfilePath: ./desktop-agent/render/Dockerfile
    dockerContext: ./desktop-agent/render
    plan: starter          # 512 MB RAM — confirmed sufficient
    region: oregon
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: "10000"
