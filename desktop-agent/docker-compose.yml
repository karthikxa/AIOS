services:
  agent:
    build: ./agent
    environment:
      - LLM_API_KEY=${LLM_API_KEY}
      - LLM_MODEL=${LLM_MODEL:-auto}
      - LLM_BASE_URL=${LLM_BASE_URL}
      - PORT=4000
    ports:
      - "4000:4000"
